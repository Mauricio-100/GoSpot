#!/bin/sh

# GoSpot Script v2 (Version Détective) pour iSH Shell
# Par Mauricio

# Couleurs pour un affichage plus clair
GREEN="\033[1;32m"
YELLOW="\033[1;33m"
RED="\033[1;31m"
RESET="\033[0m"

# Fonction pour le mode SERVEUR
run_server() {
    echo "${GREEN}--- Démarrage de GoSpot en mode SERVEUR ---${RESET}"
    echo "${YELLOW}ACTION REQUISE : Assurez-vous d'avoir activé le 'Partage de connexion' dans les Réglages de cet iPhone.${RESET}"
    
    if ! pgrep -f "sshd" > /dev/null; then
        echo "Démarrage du serveur SSH (sshd)..."
        [ ! -f "/etc/ssh/ssh_host_rsa_key" ] && ssh-keygen -A
        sshd
        echo "${GREEN}Serveur SSH démarré.${RESET}"
    else
        echo "${GREEN}Le serveur SSH est déjà en cours d'exécution.${RESET}"
    fi
    
    SERVER_IP=$(ip a | grep -o '172.20.10.1')
    echo "\n${GREEN}✅ Serveur prêt !${RESET}"
    echo "En attente d'un client qui se connecte au Partage de connexion..."
    echo "Mon adresse IP sur ce réseau est : ${GREEN}$SERVER_IP${RESET}"
}

# Fonction pour le mode CLIENT
run_client() {
    echo "${GREEN}--- Démarrage de GoSpot en mode CLIENT ---${RESET}"
    echo "${YELLOW}ACTION REQUISE : Assurez-vous d'être connecté au Wi-Fi du téléphone serveur.${RESET}\n"
    
    # --- LOGIQUE DE DÉTECTION DE CLÉ ---
    echo "🕵️  Recherche d'une clé SSH existante dans tout le terminal..."
    # On cherche les types de clés les plus courants, en ignorant les erreurs de permission
    SSH_KEY_PATH=$(find / -type f \( -name "id_ed25519" -o -name "id_rsa" \) 2>/dev/null | head -n 1)

    if [ -z "$SSH_KEY_PATH" ]; then
        echo "${YELLOW}Aucune clé SSH trouvée. Création d'une nouvelle clé (ed25519)...${RESET}"
        # Créer le dossier .ssh s'il n'existe pas
        mkdir -p "$HOME/.ssh"
        chmod 700 "$HOME/.ssh"
        # Générer une clé moderne sans mot de passe
        ssh-keygen -t ed25519 -N "" -f "$HOME/.ssh/id_ed25519"
        SSH_KEY_PATH="$HOME/.ssh/id_ed25519"
        echo "${GREEN}Nouvelle clé créée à : $SSH_KEY_PATH${RESET}"
    else
        echo "${GREEN}Clé trouvée ! Utilisation de : $SSH_KEY_PATH${RESET}"
    fi
    
    PUBLIC_KEY_PATH="${SSH_KEY_PATH}.pub"
    if [ ! -f "$PUBLIC_KEY_PATH" ]; then
        echo "${RED}ERREUR : La clé privée a été trouvée, mais sa clé publique (.pub) est manquante !${RESET}"
        echo "Essayez de la regénérer avec : ssh-keygen -y -f $SSH_KEY_PATH > $PUBLIC_KEY_PATH"
        exit 1
    fi
    # --- FIN DE LA LOGIQUE DE DÉTECTION ---

    echo "\nRecherche du serveur GoSpot sur le réseau..."
    sleep 2
    SERVER_IP=$(arp -a | grep -o '172.20.10.1')

    if [ -z "$SERVER_IP" ]; then
        echo "${RED}ERREUR : Serveur introuvable. Êtes-vous bien connecté au Partage de connexion ?${RESET}"
        exit 1
    fi
    
    echo "${GREEN}Serveur trouvé ! Adresse : $SERVER_IP${RESET}"
    echo "\nTentative de connexion avec la clé..."
    echo "${YELLOW}NOTE : La première fois, vous devrez entrer le mot de passe 'root' du serveur pour autoriser la clé.${RESET}"
    
    # On utilise la clé publique trouvée pour l'autorisation
    ssh-copy-id -o ConnectTimeout=5 -o StrictHostKeyChecking=no -i "$PUBLIC_KEY_PATH" root@$SERVER_IP
    
    if [ $? -eq 0 ]; then
        echo "${GREEN}Clé autorisée ! Connexion en cours...${RESET}"
        # On utilise la clé privée trouvée pour la connexion
        ssh -i "$SSH_KEY_PATH" root@$SERVER_IP
    else
        echo "${RED}La copie de la clé a échoué. Vérifiez le mot de passe du serveur.${RESET}"
    fi
}

# Logique principale du script
case "$1" in
    serve)
        run_server
        ;;
    connect)
        run_client
        ;;
    *)
        echo "Usage: GoS [commande]"
        echo ""
        echo "Commandes disponibles :"
        echo "  serve    : Démarre en mode serveur (sur le tel qui partage la connexion)"
        echo "  connect  : Démarre en mode client (sur le tel qui se connecte au Wi-Fi)"
        ;;
esac
